#!/bin/sh
#
# Workspace launcher
#
# Author: Thomas Frössman  ( thomasf@jossystem.se / http://thomas.jossystem.se )
#

wsname -s && exit 1

upgradeall &
(
    sleep 0.5;
    urxvt -cd ~/.opt &
    sleep 0.5;
    urxvt -cd ~/.emacs.d &
) &
