default
partial alphanumeric_keys modifier_keys 
xkb_symbols "basic" {
    name[Group1]= "Swedish (SE)";
    include "se(default)"
};

mac" {
    name[Group1]= "Swedish (SE)";
    include "se(mac)"
    include "@misc(swap_ctrl_caps)"
};

